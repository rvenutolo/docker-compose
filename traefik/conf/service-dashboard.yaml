http:
  routers:
    traefik-rtr:
      entryPoints:
        - websecure
      middlewares:
        - default-chain-mdw@file
      rule: 'Host(`traefik.{{ env "DOMAIN_NAME" }}`)'
      service: api@internal
