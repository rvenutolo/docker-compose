http:
  routers:
    traefik-rtr:
      entryPoints:
        - websecure
      middlewares:
        - default-chain-mdw
      rule: 'Host(`traefik.{{ env "DOMAIN_NAME" }}`)'
      service: api@internal
