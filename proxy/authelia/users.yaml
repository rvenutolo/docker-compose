## NOTE: generate password hashes by running:
## docker run --rm -it authelia/authelia:latest authelia crypto hash generate argon2

users:
  rvenutolo:
    disabled: false
    displayname: "Rick"
    password: {{ env "RVENUTOLO_PASSWORD_HASH" }}
    email: {{ env "RVENUTOLO_EMAIL" }}
    groups:
      - admin
