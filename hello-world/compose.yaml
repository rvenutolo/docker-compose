include:
  - ../traefik-proxy-network.yaml

services:
  hello-world:
    extends:
      file: ../service-defaults.yaml
      service: service-defaults
    image: crccheck/hello-world:latest
    container_name: hello-world
    hostname: hello-world
    volumes:
      - /etc/localtime:/etc/localtime:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.hello-world.entryPoints=websecure
      - traefik.http.routers.hello-world.rule=Host(`hello-world.${DOMAIN_NAME:?}`)
      - traefik.http.services.hello-world.loadBalancer.server.port=8000
