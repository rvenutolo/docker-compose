networks:
  proxy:
    external: true
  traefik-socket-proxy:
  traefik-logger-socket-proxy:

services:

  authelia-redis:
    image: redis:alpine
    container_name: authelia-redis
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    volumes:
      - ${DOCKER_APP_DATA_DIR:?}/proxy/redis/data:/data
      - /etc/localtime:/etc/localtime:ro
    command: redis-server --requirepass ${REDIS_PASSWORD:?}

  authelia:
    image: authelia/authelia:latest
    container_name: authelia
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    depends_on:
      - authelia-redis
    volumes:
      - ./authelia-notifications.txt:/notifications.txt
      - ./authelia/config.yaml:/config.yaml:ro
      - ./authelia/users.yaml:/users.yaml:ro
      - ${DOCKER_LOGS_DIR:?}/authelia:/var/log/authelia
      - ${DOCKER_APP_DATA_DIR:?}/proxy/authelia/config:/config
      - /etc/localtime:/etc/localtime:ro
    environment:
      DOMAIN_NAME: ${DOMAIN_NAME:?}
      DUO_API_HOSTNAME: ${AUTHELIA_DUO_API_HOSTNAME:?}
      DUO_INTEGRATION_KEY: ${AUTHELIA_DUO_INTEGRATION_KEY:?}
      DUO_SECRET_KEY: ${AUTHELIA_DUO_SECRET_KEY:?}
      SESSION_ENCRYPTION_KEY: ${AUTHELIA_SESSION_ENCRYPTION_KEY:?}
      STORAGE_ENCRYPTION_KEY: ${AUTHELIA_STORAGE_ENCRYPTION_KEY:?}
      LOG_LEVEL: ${AUTHELIA_LOG_LEVEL:-info}
      REDIS_PASSWORD: ${REDIS_PASSWORD:?}
      X_AUTHELIA_CONFIG: /config.yaml
      X_AUTHELIA_CONFIG_FILTERS: template
    labels:
      - traefik.enable=true
      - traefik.http.routers.authelia-rtr.rule=Host(`authelia.${DOMAIN_NAME:?}`)
      - traefik.http.services.authelia-svc.loadBalancer.server.port=9091

  crowdsec:
    image: crowdsecurity/crowdsec:latest
    container_name: crowdsec
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    volumes:
      - ./crowdsec/acquis:/etc/crowdsec/acquis.d:ro
      - ./crowdsec/acquis.yaml:/etc/crowdsec/acquis.yaml:ro
      - ./crowdsec/config.yaml:/etc/crowdsec/config.yaml.local:ro
      - ${DOCKER_APP_DATA_DIR:?}/proxy/crowdsec/etc/crowdsec:/etc/crowdsec
      - ${DOCKER_APP_DATA_DIR:?}/proxy/crowdsec/var/lib/crowdsec/data:/var/lib/crowdsec/data
      - ${DOCKER_LOGS_DIR:?}/authelia:/var/log/external/authelia:ro
      - ${DOCKER_LOGS_DIR:?}/traefik:/var/log/external/traefik:ro
      - /var/log/auth.log:/var/log/external/auth.log:ro
      - /var/log/kern.log:/var/log/external/kern.log:ro
      - /var/log/syslog:/var/log/external/syslog:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      COLLECTIONS: >
        crowdsecurity/appsec-crs
        crowdsecurity/appsec-generic-rules
        crowdsecurity/appsec-virtual-patching
        crowdsecurity/base-http-scenarios
        crowdsecurity/http-cve
        crowdsecurity/http-dos
        crowdsecurity/iptables
        crowdsecurity/linux
        crowdsecurity/linux-lpe
        crowdsecurity/sshd
        crowdsecurity/traefik
        LePresidente/authelia
        ${CROWDSEC_EXTRA_COLLECTIONS:-}

  crowdsec-bouncer:
    image: fbonalair/traefik-crowdsec-bouncer:latest
    container_name: crowdsec-bouncer
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    depends_on:
      - crowdsec
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      CROWDSEC_AGENT_HOST: crowdsec:8080
      CROWDSEC_BOUNCER_API_KEY: ${CROWDSEC_BOUNCER_API_KEY:?}
      CROWDSEC_BOUNCER_LOG_LEVEL: ${CROWDSEC_BOUNCER_LOG_LEVEL:-info}
      GIN_MODE: ${CROWDSEC_BOUNCER_GIN_MODE:-release}

  traefik-geoip-filter:
    image: mpdcampbell/traefik-geoip-filter:latest
    container_name: traefik-geoip-filter
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    volumes:
      - ${DOCKER_APP_DATA_DIR:?}/proxy/traefik-geoip-filter/geoip:/geoip
      - /etc/localtime:/etc/localtime:ro
    environment:
      COUNTRY_CODES: US
      EXTRA_IPS: 100.64.0.0/10 127.0.0.1/32
      FILTER_TYPE: allow
      MAXMIND_ID: ${MAXMIND_ID:?}
      MAXMIND_KEY: ${MAXMIND_KEY:?}

  traefik-socket-proxy:
    image: lscr.io/linuxserver/socket-proxy:latest
    container_name: traefik-socket-proxy
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /run
    networks:
      - traefik-socket-proxy
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_SOCKET:?}:/var/run/docker.sock
    environment:
      CONTAINERS: 1

  traefik:
    image: traefik:v3
    container_name: traefik
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
      - traefik-socket-proxy
    depends_on:
      - authelia
      - crowdsec-bouncer
      - traefik-geoip-filter
      - traefik-socket-proxy
    ports:
      - "${TAILNET_IP:?}:80:80"
      - "${TAILNET_IP:?}:443:443"
    volumes:
      - ./traefik:/etc/traefik/conf:ro
      - ${DOCKER_APP_DATA_DIR:?}/proxy/traefik/etc/traefik/certs:/etc/traefik/certs
      - /etc/localtime:/etc/localtime:ro
    environment:
      CF_DNS_API_TOKEN: ${TRAEFIK_CF_DNS_API_TOKEN:?}
      DOMAIN_NAME: ${DOMAIN_NAME:?}
    command:
      - --accessLog.filters.statusCodes=${TRAEFIK_ACCESS_LOG_STATUS_CODES:-400-499,500-599}
      - --api.dashboard=true
      - --api.disableDashboardAd=true
      - --global.sendAnonymousUsage=true
      - --log.level=${TRAEFIK_LOG_LEVEL:-INFO}
      - --certificatesResolvers.dns-cloudflare.acme.caServer=${TRAEFIK_ACME_CA_SERVER:-https://acme-v02.api.letsencrypt.org/directory}
      - --certificatesResolvers.dns-cloudflare.acme.dnsChallenge.provider=cloudflare
      - --certificatesResolvers.dns-cloudflare.acme.dnsChallenge.resolvers=1.1.1.1:53,8.8.8.8:53
      - --certificatesResolvers.dns-cloudflare.acme.storage=/etc/traefik/certs/cloudflare-acme.json
      - --entrypoints.web.address=:80
      - --entryPoints.web.forwardedHeaders.trustedIPs=${TRAEFIK_TRUSTED_IPS:-127.0.0.1/32,100.64.0.0/10}
      - --entrypoints.web.http.redirections.entrypoint.to=websecure
      - --entrypoints.websecure.address=:443
      - --entrypoints.websecure.asDefault=true
      - --entryPoints.websecure.forwardedHeaders.trustedIPs=${TRAEFIK_TRUSTED_IPS:-127.0.0.1/32,100.64.0.0/10}
      - --entryPoints.websecure.http.middlewares=default-chain-mdw@file
      - --entrypoints.websecure.http.tls.certresolver=dns-cloudflare
      - --entrypoints.websecure.http.tls.domains[0].main=${DOMAIN_NAME:?}
      - --entrypoints.websecure.http.tls.domains[0].sans=*.${DOMAIN_NAME:?}
      - --entrypoints.websecure.http.tls.options=default-tls-opts@file
      - --providers.docker.endpoint=tcp://traefik-socket-proxy:2375
      - --providers.docker.exposedByDefault=${TRAEFIK_DOCKER_EXPOSED_BY_DEFAULT:-false}
      - --providers.docker.network=proxy
      - --providers.file.directory=/etc/traefik/conf
      - --providers.file.watch=true
    labels:
      - traefik.enable=true
      - traefik.http.routers.traefik-rtr.rule=Host(`traefik.${DOMAIN_NAME:?}`)
      - traefik.http.routers.traefik-rtr.service=api@internal

  traefik-logger-socket-proxy:
    image: lscr.io/linuxserver/socket-proxy:latest
    container_name: traefik-logger-socket-proxy
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /run
    networks:
      - traefik-logger-socket-proxy
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_SOCKET:?}:/var/run/docker.sock
    environment:
      CONTAINERS: 1
      PROXY_READ_TIMEOUT: 999d

  traefik-logger:
    image: docker:dind
    container_name: traefik-logger
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - traefik-logger-socket-proxy
    depends_on:
      - traefik
      - traefik-socket-proxy
    volumes:
      - ${DOCKER_LOGS_DIR:?}/traefik:/var/log/traefik
      - /etc/localtime:/etc/localtime:ro
    environment:
      DOCKER_HOST: tcp://traefik-logger-socket-proxy:2375
    command: sh -c 'docker logs --follow traefik | tee /var/log/traefik/traefik.log'
